services:
  app:
    build: .
    ports:
      - "${VERIFY_HOST_PORT:-3000}:3000"
    environment:
      - PORT=3000
    healthcheck:
      test: ["CMD", "wget", "-q", "-O-", "http://localhost:3000/health"]
      interval: 5s
      timeout: 3s
      retries: 3
